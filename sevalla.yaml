version: '1'

services:
  - name: ifcclash
    type: web
    runtime: docker
    buildCommand: "docker build -t ifcclash ."
    healthCheckPath: "/api/health"
    envVars:
      - key: FLASK_ENV
        value: production
      - key: PORT
        value: "8080"


